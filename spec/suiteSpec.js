describe('Operação de adição', () => {
    it('Deve garantir que 1+3 é = 4', () => {
        expect((1 + 3)).toBe(4);
    });

    it('Deve garantir que a divisao seja = 1', () => {
        expect((5 / 5)).toBe(1);
    });
});